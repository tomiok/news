{{define "navbar"}}
    <nav class="navbar">
        <div class="navbar-content">
            <a href="#" class="navbar-logo">Inforia</a>
            <div class="search-container">
                <label>
                    <input type="text" class="search-input"
                           hx-get="/feeds"
                           hx-target="#news"
                           method="get"
                           name="l"
                           placeholder="lugares"
                           autocomplete="off"
                    >
                </label>
                <div class="autocomplete-list"></div>
            </div>
        </div>
    </nav>
{{end}}