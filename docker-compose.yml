version: '3'
services:
  db:
    image: mysql:8.0.32
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: news_api_dev
    ports:
      - "3306:3306"
    volumes:
      - ./mysql:/var/lib/mysql
  app:
    build: .
    restart: always
    ports:
      - "9000:9000"
    depends_on:
      - db
    environment:
      DB_HOST: db
      DB_PORT: 3306
      DB_USER: root
      DB_PASSWORD: root
volumes:
  mysql:
networks:
  my-network:
    driver: bridge